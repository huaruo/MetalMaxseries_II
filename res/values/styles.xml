<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="android:Theme.Light" />
    <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    <!-- 
    <style name="AppTheme" parent="AppBaseTheme">
    </style>
     -->
    
     <!-- Custom Title theme. -->
    <style name="customTitle" parent="android:Theme">
        <item name="android:windowTitleSize">35dip</item>
    </style>
    
    <!-- Menu name HighLight theme. -->
    <style name="menuName" parent="android:TextAppearance">
        <!-- 字体颜色 -->
        <item name = "android:textColor">@color/white</item>
        <!-- 字体大小 -->
        <item name="android:textSize">14.0sp</item>
    </style>
    
    <style name="CustomizedWindowTitleBackground">
        <item name="android:background">@drawable/top</item>
    </style>
    <style name="titlebarstyle" parent="android:Theme">
        <item name="android:windowTitleSize">48dp</item>
        <item name="android:windowTitleBackgroundStyle">@style/CustomizedWindowTitleBackground</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <!-- 
说明：
          设置控件样式可以更改控件的属性，可以通过查看android sdk目录\data\res\values 目录下的attrs.xml文件，获得控件属性的name。
           样式的parent属性说明style是可以继承的。在这里我们继承的是系统默认的EditText属性，只修改了背景和文字颜色而已。所有系统控件的样式是在一个style.xml文件中，这个文件也
在上面说的那个目录下。里面我们可以找到Widget.EditText的样式定义。
            在设置style时，我们经常用到“@”和“？”。“@”表明引用的的资源是在一个项目或是系统框架中定义过
的。“？”表明引用的资源是在当前的主题定义过的。
 -->
     <style name="et0" parent="@android:style/Widget.EditText">
        <item name="android:textColor">#000000</item>
    </style>
    <style name="et1" parent="@android:style/Widget.EditText">
        <item name="android:background">#1A4EA4</item>
        <item name="android:textColor">#FFF111</item>
        <item name="android:textSize">48dp</item>
    </style>
    <style name="et2" parent="@android:style/Widget.EditText">
        <item name="android:background">#A6C60F</item>
        <item name="android:textColor">#EC02C3</item>
    </style>
    
    <!-- 
       	说明：
       	下面的两个主题样式，第一个是对话框的主题，第二个是壁纸的主题。如果需要更改主题的某些属性，可以参照
       	android sdk目录\data\res\values 目录下的themes.xml文件中的主题样式进行更改。
     -->
    <style name="MyTheme1" parent="@android:style/Theme.Dialog">
    </style>
    <style name="MyTheme2" parent="@android:style/Theme.Wallpaper.NoTitleBar.Fullscreen">
    </style>
     <style name="CustomWindowTitleBackground">
        <item name="android:background">#323331</item>
    </style>

    <style name="CustomTheme" parent="android:Theme">
        <item name="android:windowTitleSize">35dip</item>
        <item name="android:windowTitleBackgroundStyle">@style/CustomWindowTitleBackground</item>
    </style>
	<style name="MidalButtonTextStyle" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>   
        <item name="android:padding">6dp</item>
        <item name="android:textColor">@drawable/keypad_button_text_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>
    </style>

</resources>